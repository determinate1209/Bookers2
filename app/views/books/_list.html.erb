<h2>User info</h2>
  <% if user = current_user %>
    
        <%= image_tag user.get_profile_image(100,100) %>
        <p>name<%= user.name %></p>
        <p>introduction <%= user.introduction %></p>
      </tr>
    <%= link_to " a ", edit_user_path(user) %>
  <%end%>
    
<h2>New book</h2>

<%= form_with model: book do |f| %>
  <div>
    <label>Title</br>
      <%= f.text_field :title %>
    </label>  
	</div>
  <div>
    <label>Opinion</br>
     <%= f.text_area :body %></br>
	  </label>
  </div>
  <div>
    <%= f.submit 'Create Book' %>
  </div>  
<% end %>